import { EgyptianIdDatePipe } from './egyptian-id-date-pipe.pipe';

describe('EgyptianIdDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new EgyptianIdDatePipe();
    expect(pipe).toBeTruthy();
  });
});
