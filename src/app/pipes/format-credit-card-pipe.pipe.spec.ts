import { FormatCreditCardPipePipe } from './format-credit-card-pipe.pipe';

describe('FormatCreditCardPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCreditCardPipePipe();
    expect(pipe).toBeTruthy();
  });
});
